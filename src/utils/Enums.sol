// SPDX-License-Identifier: MIT
pragma solidity >=0.8.4;

enum DisplayType {
    String,
    Number,
    Date,
    BoostPercent,
    BoostNumber
}

enum LayerType {
    PORTRAIT,
    BACKGROUND,
    TEXTURE,
    OBJECT,
    BORDER
}